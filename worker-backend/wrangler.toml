name = "parking-lot-navigator-api"
main = "src/index.ts"
compatibility_date = "2026-04-17"
compatibility_flags = ["nodejs_compat", "nodejs_compat_populate_process_env"]
minify = true

# Worker 로그를 Cloudflare에 보존한다. 없으면 wrangler tail(실시간) 말고는 과거 로그를
# 조회할 방법이 없어서, 알림 중복 같은 사후 조사에서 발송 시각·claim_id·APNs 응답을
# 되짚을 수 없다. upcomingNotifications의 logAttempt가 남기는 구조화 로그가 여기 쌓인다.
[observability]
enabled = true
head_sampling_rate = 1

[vars]
NODE_ENV = "production"
LOG_LEVEL = "info"
PARKING_PROVIDER_MODE = "real"
DEFAULT_SEARCH_RADIUS_METERS = "800"
DEFAULT_DISCOVER_RADIUS_METERS = "20000"
STALE_THRESHOLD_SECONDS = "600"
CACHE_TTL_SECONDS = "45"
DISCOVER_CACHE_TTL_SECONDS = "21600"
DISCOVERY_SYNC_CONCURRENCY = "4"
DISCOVERY_SYNC_FETCH_TIMEOUT_MS = "20000"
FESTIVAL_PROVIDER_ENABLED = "true"
EVENT_PROVIDER_ENABLED = "true"
LOCAL_EVENT_PROVIDER_ENABLED = "true"
LOCAL_EVENT_AUTO_APPROVE_MIN_SCORE = "0.75"
LOCAL_EVENT_SEARCH_MAX_QUERIES = "17"
LOCAL_EVENT_MAX_KAKAO_LOOKUPS = "30"
LOCAL_EVENT_BLOG_DISPLAY = "20"
LOCAL_EVENT_KAKAO_RADIUS_METERS = "20000"
AGENT_HEAD_INCLUDE_REJECTED = "true"
AGENT_HEAD_BATCH_SIZE = "15"
AGENT_HEAD_MAX_BATCHES = "3"
AGENT_PIXEL_ENABLED = "true"
AGENT_PIXEL_BATCH_SIZE = "12"
NAVER_SEARCH_BASE_URL = "https://openapi.naver.com"
KAKAO_LOCAL_BASE_URL = "https://dapi.kakao.com"
SEOUL_OPEN_DATA_BASE_URL = "http://openapi.seoul.go.kr:8088"
PUBLIC_DATA_ENV = "production"
PUBLIC_DATA_BASE_URL = "https://apis.data.go.kr"
NATIONAL_PARKING_DATA_BASE_URL = "https://api.data.go.kr"
KOPIS_BASE_URL = "http://www.kopis.or.kr"
KOPIS_MAX_PAGES = "10"
KOPIS_DETAIL_MAX_ITEMS = "5"
# 10페이지(1000행)씩 3구간을 돌아가며 읽어 전체 공연을 순회한다
KOPIS_PAGE_CYCLES = "3"
FEE_BACKFILL_MAX_ITEMS = "45"
# backfill 세 종류가 `*/5` cron의 분 슬롯을 따로 쓰게 되면서 회차마다 subrequest
# 50건 예산을 통째로 쓴다. 항목당 외부 fetch 1건이므로 45까지 올려도 여유가 남는다.
IMAGE_BACKFILL_MAX_ITEMS = "45"
GEOCODE_BACKFILL_MAX_LOOKUPS = "45"
KCISA_BASE_URL = "https://api.kcisa.kr"
KCISA_MAX_PAGES = "5"
EVENT_GEOCODE_MISS_BUDGET = "20"
CITY_FESTIVAL_GEOCODE_MISS_BUDGET = "15"
CITY_FESTIVAL_DETAIL_FETCH_BUDGET = "15"
MERCHANT_PUBLIC_BASE_URL = "https://parking-lot-navigator-api.parkingnav.workers.dev"
TOSS_CLIENT_KEY = "test_gck_docs_OAQ92ymxN34nW1Gq9DRBaajRKgw6"
TAGGING_MODEL = "@cf/meta/llama-3.3-70b-instruct-fp8-fast"
TAGGING_BATCH_SIZE = "20"
TAGGING_RUN_MAX_ROWS = "200"
TAGGING_CONCURRENCY = "2"
# 다가오는 행사 알림 한 회차 APNs 호출 상한. 항목당 외부 fetch 1건이고 invocation당
# 50건 한도라 40을 넘기지 않는다. 못 보낸 대기 행은 다음 회차에 그대로 다시 잡힌다.
UPCOMING_NOTIFICATION_MAX_PUSHES = "40"
APNS_BUNDLE_ID = "com.sangminbis9.ParkingLotNavigator"

# cron 하나로 합쳤다. 계정당 cron trigger가 5개뿐이라 예전에는 그 다섯을 전부 썼고,
# 새 스케줄을 얹을 자리가 없었다. 분 가드로 기존 빈도를 그대로 재현한다
# (index.ts runMinuteScheduler 참고). 하루 1,440 invocation은 무료 한도 100,000의 1.4%다.
[triggers]
crons = ["* * * * *"]

# producer와 consumer가 같은 스크립트다(새 Worker 프로젝트를 만들지 않는다).
[[queues.producers]]
binding = "BACKGROUND_QUEUE"
queue = "parkingnav-background"

# max_batch_size = 1: op는 64KB 단위라 배치로 묶어도 줄지 않는다. 묶는 대신
# 메시지마다 CPU 10ms / subrequest 50건 예산을 온전히 준다.
# max_retries = 2: CPU 초과로 죽은 회차는 ack되지 않아 두 번까지 재배달되고,
# 그래도 안 되면 버린다 — 프로그램 크롤은 D1의 15분 선점이 만료되며 자연히 다시 잡힌다.
[[queues.consumers]]
queue = "parkingnav-background"
max_batch_size = 1
max_batch_timeout = 5
max_retries = 2

[[d1_databases]]
binding = "DB"
database_name = "parking-lot-navigator"
database_id = "31c04846-57d5-4e38-82b6-2d7b3a0dfbee"

[[r2_buckets]]
binding = "MERCHANT_IMAGES"
bucket_name = "merchant-images"

[ai]
binding = "AI"
