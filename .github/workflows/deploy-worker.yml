name: Deploy Worker

on:
  push:
    branches:
      - master
    paths:
      - "backend/**"
      - "shared-types/**"
      - "worker-backend/**"
      - "package.json"
      - "package-lock.json"
      - "pnpm-workspace.yaml"
      - ".github/workflows/deploy-worker.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      SYNC_ADMIN_TOKEN: ${{ secrets.SYNC_ADMIN_TOKEN }}
      NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
      NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
      KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Enable pnpm
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Typecheck Worker
        working-directory: worker-backend
        run: pnpm exec tsc --noEmit

      - name: Sync Worker API secrets
        working-directory: worker-backend
        run: |
          if [ -n "${NAVER_CLIENT_ID}" ]; then
            printf "%s" "${NAVER_CLIENT_ID}" | pnpm exec wrangler secret put NAVER_CLIENT_ID
          fi
          if [ -n "${NAVER_CLIENT_SECRET}" ]; then
            printf "%s" "${NAVER_CLIENT_SECRET}" | pnpm exec wrangler secret put NAVER_CLIENT_SECRET
          fi
          if [ -n "${KAKAO_REST_API_KEY}" ]; then
            printf "%s" "${KAKAO_REST_API_KEY}" | pnpm exec wrangler secret put KAKAO_REST_API_KEY
          fi

      - name: Deploy Worker
        working-directory: worker-backend
        run: pnpm exec wrangler deploy

      - name: Refresh discovery cache
        if: ${{ env.SYNC_ADMIN_TOKEN != '' }}
        run: |
          curl -fsS -X POST \
            -H "Authorization: Bearer ${SYNC_ADMIN_TOKEN}" \
            "https://parking-lot-navigator-api.parkingnav.workers.dev/admin/sync-discovery?kinds=festivals,events"

      - name: Refresh local event candidates
        if: ${{ env.SYNC_ADMIN_TOKEN != '' }}
        run: |
          curl -fsS -X POST \
            -H "Authorization: Bearer ${SYNC_ADMIN_TOKEN}" \
            "https://parking-lot-navigator-api.parkingnav.workers.dev/admin/sync-local-events"
